import React from "react";
import styled from "styled-components";

const StyledFooter = styled.div``;

export default function CardFooterComponent() {
  return <StyledFooter>CardFooterComponent</StyledFooter>;
}
